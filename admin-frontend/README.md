# Valerion Admin Frontend

This is a Vite + React + TypeScript admin dashboard scaffold with Tailwind CSS. It connects to the Valerion HRMS backend.

Run locally:

```bash
cd admin-frontend
npm install
npm run dev
```

Configure backend base URL via environment variable: create `.env` with `VITE_API_URL=http://localhost:4000/api`.
